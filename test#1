local GameName = ""
local MarketplaceService = game:GetService("MarketplaceService")
local success, gameInfo = pcall(function()
    return MarketplaceService:GetProductInfo(game.PlaceId)
end)
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
if success then
    GameName = gameInfo.Name
else
    warn("Failed to get game name")
end

local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/reallycoolusername101-cmyk/Nil_Library/refs/heads/main/wizard"))()
	local MainWindow = Library:NewWindow("NIL HUB -  " .. GameName)
	local MainSection = MainWindow:NewSection("Main")


    MainSection:CreateButton("Give inf money",function()
     for h,i in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Main.Frame["Main Body"].Main.Shop.Cars.CarList.CarList:GetChildren()) do

local args = {
    -1e999,
    i.Name,
    "purchase"
}
game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("CarShop"):FireServer(unpack(args))
end
    end)




function getplot()
    for i, plot in pairs(workspace.Tycoons:GetChildren()) do
        if plot.Values.Owner.Value == game.Players.LocalPlayer.Name then
            return plot
        end
    end
end


local getplot = getplot()
print(getplot.Name)
local active = false
MainSection:CreateToggle("AutoUpgrade",function(arg)
active = arg

task.spawn(function()
    if active == false then return end

    while active do 







for i, button in pairs(getplot.Buttons:GetChildren()) do
    print(button.Name)
    
    -- Fixed: Changed 'or' to 'and' so it must exclude ALL of these names
    if button:FindFirstChild("Head") 
        
       and button.Head.Color ~= Color3.fromRGB(255, 0, 255) 
       and button.Name ~= "Cash" 
       and button.Name ~= "VIP" 
       and button.Name ~= "Auto"
       and button.Name ~= "PC 1"
       and not button:FindFirstChild("Gamepass" )
        then
 




        print("Valid")
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = button.Head.CFrame
        break 
    end
end









task.wait()






end
end)
end)











